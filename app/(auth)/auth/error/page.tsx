import { ErrorCard } from "@/components/auth/error-card";

interface AuthErrorPageProps {}

const AuthErrorPage = (props: AuthErrorPageProps) => {
  return <ErrorCard />;
};
export default AuthErrorPage;
